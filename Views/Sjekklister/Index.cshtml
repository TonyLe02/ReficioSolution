@model IEnumerable<ReficioSolution.Models.CheckListViewModel>

@{
    ViewData["Title"] = "Sjekklist oversikt";
}

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sjekklister</title>
    <link rel="stylesheet" href="/css/ServiceOrder.css">

</head>

<body>
<h2>Sjekklist oversikt</h2>
    <form>
        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th>Checklist Id</th>
                <th>Sign</th>
                <th>Freeform</th>
                <th>CompletionDate:</th>
             
            </tr>
            </thead>
            <tbody>
            
            @foreach (var Checklist in Model)
            {
                <tr>
                    <td>
                        <a class="nav-link text-dark" asp-area="" asp-controller="FilledOutCheckList" asp-action="Index" asp-route-id="@Checklist.ChecklistId">
                            @Checklist.CheckpointId
                        </a>
                    </td>
                    <td>
                        <a class="nav-link text-dark" asp-area="" asp-controller="FilledOutCheckList" asp-action="Index" asp-route-id="@Checklist.ChecklistId">
                            @Checklist.Sign
                        </a>
                    </td>
                    <td>
                        <a class="nav-link text-dark" asp-area="" asp-controller="FilledOutCheckList" asp-action="Index" asp-route-id="@Checklist.ChecklistId">
                            @Checklist.Freeform
                        </a>
                    </td>
                    <td>
                        <a class="nav-link text-dark" asp-area="" asp-controller="FilledOutCheckList" asp-action="Index" asp-route-id="@Checklist.ChecklistId">
                            @Html.DisplayFor(model => Checklist.CompletionDate)
                        </a>
                    </td>
                </tr>
            }
            
            </tbody>
        </table>
    </form>
</body>